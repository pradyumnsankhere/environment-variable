name: Example Workflow

on:
  push:
    branches:
      - master

jobs:
  example-job:
    runs-on: ubuntu-latest

    env:
      MY_VARIABLE: MY_VARIABLE

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Use Environment Variable
      run: echo "My  vlue---${{ env.MY_VARIABLE }}"
